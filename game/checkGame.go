package game

// CheckGame checks if the previous game exists and its status is IN PROGRESS
// cookies token has a GameId and WonBy
// If no cookies are set we RETURN new GAME only
// if GameId is found we check teh status, if in Progress we return RESUME is available
func CheckGame() bool {

	//game exists we say yes
	// otherwise no

	return false

}
